/* eslint func-names: "off" */
/* eslint prefer-arrow-callback: "off" */

const chai = require('chai');

const expect = chai.expect;

describe('user streams', function() {
  it('should respond to a direct mention with "hello world"');
});
