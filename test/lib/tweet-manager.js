/* eslint func-names: "off" */

const debug = require('debug')('tb:tweet-manager');

module.exports = (function() {
  const testTweets   = [];
  const tweetManager = {
    tweet,
  };
  
  
  function tweet(bot, ) {
    
  }
  
  
  return tweetManager;
})();
